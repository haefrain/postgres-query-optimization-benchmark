# Copy to .env — defaults match docker-compose.yml
DATABASE_URL=postgresql://bench:bench@localhost:5434/bench

# Scales the seed. 1 = full dataset (~1.5M orders). Use a small value for a quick
# local check or in CI (e.g. SEED_SCALE=0.02 ≈ 30k orders).
SEED_SCALE=1
